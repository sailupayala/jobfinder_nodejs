var expect=require("chai").expect;
describe('get jobs',function(){
    it("it would never empty",function(){
        jobsList=[];
        
        expect(jobsList.length).to.be.at.least(1);
        
    });
});